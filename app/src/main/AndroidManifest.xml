<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bangproject.reactlayout">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <!--        screenSize : 앱의 화면방향이 전환 될 때 앱이 다시 실행되지 않게 해줌 (13버전부터 적용가능)-->
        <!--        orientation : 앱의 화면방향이 전환 될 때 앱이 다시 실행되지 않게 해줌 -->
        <!--        keyboardHidden : 키보드 가용성이 변경되었을 때 앱이 다시 실행되지 않게 해줌 -->
        <!--        앱에서 구성 변경을 수동으로 처리하려면 orientation|screenSize 를 같이 적용 하면된다.-->
        <!--        그럼 onConfigurationChanged을 받을 수 있다.-->
        <!--        앱에서 구성 변경을 자동으로 처리하려면 orientation|keyboardHidden 를 같이 적용 하면된다.-->
        <!--        그럼 액티비티는 재생성된다.-->
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>